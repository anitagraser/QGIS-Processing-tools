{"ALG_DESC": "This script uses the QGIS network analysis library to route between the nodes of all the input line layer. For each input line, one output route will created.", "network": "The line layer containing network layer which should be used to route on. ", "ALG_CREATOR": "Anita Graser", "lines": "A line layer. For each input line, the script will route between its nodes and create one output route. ", "line_id_field": "The line layer field/attribute which contains the line id. ", "ALG_VERSION": "", "ALG_HELP_CREATOR": "Anita Graser", "routes": "A line layer with the resulting routes. It will contain one route per input line."}